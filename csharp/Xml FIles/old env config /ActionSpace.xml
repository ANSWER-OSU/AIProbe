<ActionSpace type="Discrete">
    <Actions>
        <Action id="0" name="forward" >
            <Description>Move the agent one step forward</Description>
        </Action>
        <Action id="1" name="left" >
            <Description>Turn the agent 90 degrees left</Description>
        </Action>
        <Action id="2" name="right" >
            <Description>Turn the agent 90 degrees right</Description>
        </Action>
        <Action id="3" name="pick_up" >
            <Description>Pick up an object if one is present</Description>
        </Action>
        <Action id="4" name="drop">
            <Description>Drop the currently held object</Description>
        </Action>
        <Action id="5" name="toggle" >
            <Description>Toggle the state of an object (e.g., open a door)</Description>
        </Action>
    </Actions>
</ActionSpace>